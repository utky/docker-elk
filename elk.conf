[supervisord]
nodaemon=true

[program:elasticsearch]
command=/var/lib/elasticsearch/bin/elasticsearch -Des.config=/conf/elasticsearch.yml

[program:logstash]
command=/var/lib/logstash/bin/logstash agent -f /conf/logstash.conf web
